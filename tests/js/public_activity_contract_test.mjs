import assert from "node:assert/strict"; import fs from "node:fs/promises"; import path from "node:path"; const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),"../.."); const source=await fs.readFile(path.join(root,"addons/odoo_ai_assistant/static/src/services/assistant_public_activity_contract.js"),"utf8"); const contract=await import(`data:text/javascript;base64,${Buffer.from(source).toString("base64")}`); const event=(sequence,overrides={})=>({sequence,turn_id:"turn-public-0001",kind:"capability.started",phase:"capability",status:"running",label:"Consultando sale.order",resource:{model:"sale.order",record_ids:[7],display_names:["S0007"]},capability:"odoo.query_records",progress:null,diagnostic_code:null,occurred_at:"2026-08-28T10:00:00.000000Z",...overrides}); assert.equal(contract.normalizePublicTurnEvent(event(1)).kind,"capability.started"); assert.equal(contract.normalizePublicTurnEvent(event(1,{kind:"agent.thinking"})),null); assert.equal(contract.normalizePublicTurnEvent({...event(1),payload:{prompt:"private"}}),null); assert.deepEqual(contract.normalizePublicTurnEventBatch([event(4),event(5)],{afterSequence:3}).map(x=>x.sequence),[4,5]); assert.equal(contract.normalizePublicTurnEventBatch([event(5),event(4)],{afterSequence:3}),null); console.log("public activity contract: 5 assertions passed");
