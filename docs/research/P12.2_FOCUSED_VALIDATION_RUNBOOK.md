# P12.2 focused validation runbook

State: `READY / NOT EXECUTED`  
Scope: typed private-workspace patch/diff contract plus the post-P11 spreadsheet upload
regression discovered during P12 work.

## 1. Dependency-light source patch gate

Run on the exact committed `main`:

```text
python -m compileall -q \
  addons/odoo_ai_assistant/runtime/source_workspace.py \
  addons/odoo_ai_assistant/runtime/source_patch.py \
  addons/odoo_ai_assistant/runtime/capabilities/providers/technical_source_workspace.py \
  tests/unit/test_phase12_source_workspace.py \
  tests/unit/test_phase12_source_patch.py

python -m unittest -v \
  tests.unit.test_phase12_source_workspace \
  tests.unit.test_phase12_source_patch
```

Run focused Ruff on the same Python surfaces plus the changed test/manifest/model files.

Required P12.2 properties:

- preview is deterministic and leaves the parent untouched;
- exact expected workspace fingerprint is mandatory;
- installed-source staleness blocks preview/apply;
- logical traversal, absolute, VCS, secret-like and unsupported/binary paths fail closed;
- `modify` requires one exact old-fragment match;
- approval diff is complete or the proposal is rejected, never truncated;
- apply creates a derived workspace and leaves the parent unchanged;
- before/after/diff/approval fingerprints remain inspectable in the receipt;
- binding cannot transfer across user/company/database/turn;
- no installed source write, shell, Git or command execution occurs.

## 2. Focused Odoo patch gate — HARD before P12.3

Update the addon on a disposable Odoo 18 database and execute:

```text
/odoo_ai_assistant:TestPhase12SourceWorkspace
/odoo_ai_assistant:TestPhase12SourcePatch
```

Expected selected methods: 6 total (3 + 3).

Pass requires Technical-only discovery, PLAN/POLICY lifecycle on apply, readable bounded
logical source, complete preview diff, derived workspace receipt, source immutability
and cross-binding/stale denial.

If this gate fails, repair P12.2 before introducing a test-execution or deployment
capability.

## 3. Spreadsheet/chat regression discovered during P12

Because the user-facing attachment transport was found to block Excel, also run:

```text
/odoo_ai_assistant:TestPhase11SpreadsheetImport
/odoo_ai_assistant:TestPhase11DataImportSession
```

Then exercise the browser composer with a real `.xlsx` attachment.

Pass requires:

- `.xlsx` selectable from the paperclip input;
- authenticated upload succeeds under the same 8 MiB ceiling;
- the file is bound to the new durable turn;
- `assistant.data_import.inspect_file` sees native workbook headers;
- `assistant.data_import.start_file` imports through the existing durable chunk path;
- prior CSV import behavior remains green;
- attaching a spreadsheet alone does not persist it into Company Knowledge.

Odoo 18's standard import path is the compatibility reference: Excel `.xls/.xlsx` and
CSV are documented import formats, and native `base_import` owns spreadsheet parsing.

## 4. Real P12 gates

P12.2 does not authorize P12 acceptance. The later named gates remain:

```text
P12-REAL-PATH-BOUNDARY
P12-REAL-DIFF-APPROVAL
P12-REAL-TEST-BEFORE-DEPLOY
P12-REAL-DEPLOY-VERIFY
P12-REAL-FAILED-DEPLOY-RECOVERY
```

After the focused P12.2 gate is green, execute `P12-REAL-DIFF-APPROVAL` on a disposable
fixture workspace before P12.3 if the active execution state requires it. P12.3 and
P12.4 remain necessary for test-before-deploy and production recovery gates.

Do not record any unexecuted gate as PASS.
