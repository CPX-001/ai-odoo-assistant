# P7.1 CapabilityProvider foundation — focused validation

Date: 2026-08-31

## Lineage

```text
BASE_SHA: 3c9e1186413214bebaaff52dcc2836d7d907e5f7
TESTED_SHA: f1636401fe21d3c28b6afef76aa0a6c9765125ce
FINAL_SHA: evidence publication commit (resolve with git log -1 -- this file)
```

The run started with `git checkout main`, `git pull --ff-only`, `git status --short` and
`git rev-parse HEAD`. The pulled checkout was clean at `BASE_SHA`.

## Result

```text
P7.1 isolated CapabilityProvider foundation: PASS
Live effective-catalog integration: NOT EXECUTED / blocked by Product Behavior Evals v1
P7 real gates: NOT EXECUTED
P7.2 and later: NOT STARTED
```

The focused contract covers static provider composition, decorated handlers, duplicate provider
identity rejection, optional failure isolation, required-provider fail-closed behavior, capability
collision rejection without core shadowing, invalid optional dependency fallback, and deterministic
direct Odoo-registry marker discovery.

## Commands and totals

```bash
.venv/bin/python -m pytest -q tests/unit/test_capability_provider_extensions.py
.venv/bin/python -m py_compile \
  addons/odoo_ai_assistant/runtime/capabilities/provider.py \
  addons/odoo_ai_assistant/runtime/capabilities/registry.py \
  addons/odoo_ai_assistant/runtime/capabilities/__init__.py \
  tests/unit/test_capability_provider_extensions.py
.venv/bin/ruff check \
  addons/odoo_ai_assistant/runtime/capabilities/provider.py \
  addons/odoo_ai_assistant/runtime/capabilities/registry.py \
  addons/odoo_ai_assistant/runtime/capabilities/__init__.py \
  tests/unit/test_capability_provider_extensions.py
git diff --check
```

Final results:

```text
pytest: 8 passed in 0.03s
py_compile: PASS
ruff: PASS
git diff --check: PASS
```

## Repair

The first Ruff run reported `UP037` on the `CapabilityProvider.from_objects` return annotation.
Because the module already uses `from __future__ import annotations`, the redundant string quotes
were removed. This does not change runtime semantics. The complete focused command set was then
rerun and passed.

No full repository, addon, HOOT, real-provider or historical regression was authorized or run.
