# P5.3 bounded deterministic regression

Date: 2026-08-29
Validation ID: `P5.3-DETERMINISTIC-REGRESSION`
Tested checkpoint: `5253181`
Result: `PASS`

## Environment

The gate ran locally against a newly installed disposable `odoo_ai_*` database using Odoo 18
Community, PostgreSQL 16.15 and Python 3.12.3. The addon was explicitly updated at the tested
checkpoint while executing the selected methods.

The first pre-test installation invocation used the wrong operating-system identity and PostgreSQL
peer authentication rejected it before a database was created. The local harness invocation was
corrected to run Odoo as its configured system user; no project code, assertion or test selector was
changed.

No provider process or browser gate was part of this deterministic slice. No credentials, prompts,
answers, provider events or disposable identifiers are retained.

## Executed contracts

```text
TestAssistantTurnQueue.test_enqueue_persists_user_turn_and_initial_event
TestAssistantTurnQueue.test_enqueue_is_idempotent_for_client_request_id
TestAssistantUserPreferences.test_reasoning_model_is_owned_by_current_user
TestAssistantChatPolicy.test_effective_capability_policy_uses_user_autonomy_once
TestCapabilityActions.test_approve_requeues_same_turn_with_bound_authorization
TestAssistantTurnSettingsSnapshot.test_runtime_codex_settings_use_captured_turn_model
```

Recorded Odoo result:

```text
6 selected tests, 0 failures, 0 errors
Odoo internal counter: 16 executions, 971 queries
```

The passing contracts cover normal enqueue persistence, client-request idempotency, per-user model
preference isolation, autonomy-profile policy resolution, approval requeue of the same persisted
turn under its captured model/policy, and runtime consumption of the persisted turn model.

No project failure or repair was required. The disposable database, filestore and temporary logs
were removed. The original Odoo service was restored and returned HTTP 200.

## Exit decision

The bounded deterministic regression is complete. P5.3 is not yet COMPLETE: the formal
`P5-REAL-SETTINGS-SNAPSHOT` product-path gate and the full-addon regression boundary remain pending.
P5.4 remains blocked.
