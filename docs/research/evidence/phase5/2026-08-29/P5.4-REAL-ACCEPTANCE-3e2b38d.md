# P5.4 real acceptance — final activity / answer / failure UX

Date: 2026-08-29
Result: `PASS`
Repository state before pull: `934a65614ffbbcbe9b976ac361a6ccf98ace23ae`
Pulled validation baseline: `38cf302b9d0ee6cc5ac8452611f76babf091295b`
Exact content-tested checkpoint: `3e2b38d68fe172cd2cf92d7794159f73476ac23d`

## Environment

The final grouped battery ran locally on one disposable `odoo_ai_*` Odoo 18 Community database,
updated to the exact tested checkpoint before the final regression and browser gates. The host used
PostgreSQL 16.15, Python 3.12.3, Node 18.19.1, Playwright 1.55.0, headless Chromium 140.0.7339.16
and Codex CLI 0.144.2. The provider-owned Codex session under Odoo's `data_dir` reported
`authenticated` before the real gates.

The installed service was stopped while the isolated loopback Odoo process owned port 8069. No
credentials, cookies, tokens, raw prompts or answers, provider payloads, policy payloads, database
names, record identifiers or turn identifiers are retained here.

## Deterministic, Odoo and HOOT results

```text
P5.4-DETERMINISTIC-FINAL-UX   PASS
  Python unit suite           222 passed
  failure contract            14 assertions passed
  public activity contract    5 assertions passed
  Node runner syntax          PASS (basic chat + approval)
  gate manifest JSON          PASS
  compileall / diff check     PASS

P5.4-FULL-ADDON-REGRESSION    PASS
  Odoo public total           126 tests, 0 failures, 0 errors
  Odoo internal executions    174 tests

P5.4-HOOT-REGRESSION          PASS
  @odoo_ai_assistant          101 tests / 392 assertions
```

The final isolated server recorded only the three expected test-fixture terminal errors used by the
error/recovery gates; there were no unplanned `ERROR` or `CRITICAL` entries.

## HARD real gates

```text
P5-REAL-CHAT-BASIC     PASS
P5-REAL-ERROR-UX       PASS
P5-REAL-APPROVAL-UX    PASS
P5-REAL-RECOVERY-UX    PASS
```

The bounded browser observations were reviewed before formal acceptance:

- the user message rendered in 73 ms, before enqueue completed;
- real public activity appeared outside Assistant prose, suppressed the fake `Pensando…` bubble,
  settled independently and left exactly one final Assistant message;
- deterministic timeout remained an explicit safe-retry failure and deterministic capability
  failure remained an explicit review-required failure;
- recovery remained `recovery_required`, effect state `unknown`, retryability `never`, with no blind
  replay control;
- a real current-runtime `odoo.record.patch` turn reached `awaiting_confirmation` on a disposable
  partner under the strict profile;
- approval rendered as a dedicated state with visible step, capability and risk information;
- changing autonomy while approval was pending preserved the same persisted turn;
- both rejection and continuation sent their decision for that exact turn; no business write
  occurred before the decision, and the disposable field was restored after the continuation run.

## Failures found and repairs

1. The P5.4 runbook specified a HARD real approval observation but had no reproducible current-UI
   runner. `tests/e2e/p5_4_approval_browser.mjs` now exercises the supported Odoo-native turn and
   plan-decision routes with a bounded disposable `res.partner` field; the machine gate manifest now
   names that command.
2. The first draft of that new runner expected the browser plan at the top level of turn status.
   The current status contract carries it under the validated response. The harness was corrected at
   the smallest layer and both reject/approve trials were rerun successfully.
3. This machine requires a disposable Settings user for `/web/tests`; the normal internal fixture
   cannot read module-dependency metadata. HOOT therefore used the disposable database administrator,
   while product browser gates continued under the dedicated non-superuser fixture.

No production runtime defect was found in the final validation, and no product assertion or gate was
weakened.

## Cleanup

The isolated Odoo process was stopped, the disposable database and its filestore were removed, and
temporary Playwright dependencies plus raw local logs/traces were deleted. The normal Odoo service
was restored and returned loopback HTTP 200. The removed database contained validation-only data and
is not recoverable.

## Exit decision

All three local gates and all four HARD real gates passed on the coherent content lineage through
`3e2b38d`. P5.4 is `COMPLETE`. P5.5 was not started by this validation run.
