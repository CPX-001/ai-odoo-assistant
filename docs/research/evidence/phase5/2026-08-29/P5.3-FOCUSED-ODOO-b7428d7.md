# P5.3 focused Odoo validation — stable settings snapshot

Date: 2026-08-29
Validation ID: `P5.3-ODOO-SETTINGS-SNAPSHOT`
Tested checkpoint: `b7428d7`
Result: `PASS`

## Environment

The focused gate ran locally against a newly installed disposable `odoo_ai_*` database using Odoo
18 Community, PostgreSQL 16.15 and Python 3.12.3. The addon was then explicitly updated at the
tested checkpoint before executing the focused class.

No provider or browser gate was required for this deterministic slice. No credentials, prompts,
answers, provider events or disposable identifiers are retained.

## Command shape and result

```text
odoo-bin -c <local-config> -d <disposable-odoo_ai-db> \
  -u odoo_ai_assistant \
  --test-enable \
  --test-tags '/odoo_ai_assistant:TestAssistantTurnSettingsSnapshot' \
  --stop-after-init --no-http --log-level=test

2 tests, 0 failures, 0 errors
Odoo internal counter: 4 executions, 246 queries
```

Observed contract:

- Turn A captured snapshot format v1 with synthetic model A, strict autonomy and
  `always_confirm` user policy;
- after changing preferences, Turn A retained its original snapshot;
- Turn B captured synthetic model B, full-access autonomy and protected maximum auto risk;
- `reasoning_model`, `policy_payload` and `execution_settings_payload` were immutable after
  capture, including for the technical test user;
- an unrelated lifecycle field remained writable and the snapshot still validated afterward.

No failure or repair was required. The disposable database, filestore and temporary logs were
removed, and the original Odoo service was restored and returned HTTP 200.

## Exit decision

The first mandatory P5.3 gate is complete. P5.3 is not yet COMPLETE: its bounded deterministic
regression slice and formal `P5-REAL-SETTINGS-SNAPSHOT` gate remain pending. P5.4 remains blocked.
