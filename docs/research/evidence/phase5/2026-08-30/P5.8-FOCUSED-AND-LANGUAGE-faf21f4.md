# P5.8 focused tests and response-language prompt gate

Date: 2026-08-30  
Checkpoint tested: `faf21f4809cf04020e795a8b824b3197b56c4ace`  
Result: **PASS**

## Scope

This rerun intentionally covers only the newly added P5.8 contracts and the real
response-language prompt redirection gate requested for this checkpoint. It does not accept the
complete P5.8 real semantic/i18n/navigation/batch/reconnect chain.

Environment:

- Odoo 18 Community;
- PostgreSQL 16.15;
- authenticated host-primary Codex session with GPT-5.6;
- Codex in-app Chromium browser;
- disposable `odoo_ai_*` database and conversations only.

No credentials, raw prompts/answers, private reasoning, provider output or customer data are
retained.

## Focused results

```text
dependency-light/unit regression                         PASS (231 tests)
failure contract JavaScript                              PASS (14 assertions)
public-activity JavaScript                               PASS (8 assertions)
focused P5.8 + response-language Odoo selector           PASS (26 tests, 0 failures/errors)
assistant_semantic_activity HOOT                         PASS (9 tests, 32 assertions)
assistant_activity_preferences HOOT                      PASS (3 tests, 11 assertions)
assistant_live_stream_client HOOT                        PASS (3 tests, 8 assertions)
assistant_reasoning_summary_service HOOT                 PASS (4 tests, 10 assertions)
assistant_public_reference_service HOOT                  PASS (5 tests, 22 assertions)
P5-REAL-LANGUAGE-PREFERENCE                              PASS
```

The focused HOOT total is 24 tests and 83 assertions. The five suite sources are byte-identical to
the earlier focused checkpoint, and every suite was rerun through the Odoo HOOT UI on this exact
checkpoint. The focused Odoo selector included activity preferences, capability/activity
correlation, readable reasoning-summary boundaries and projection, public references, Phase-3
public projection, plus the three response-language snapshot/validation/effect cases.

## Real response-language observation

Through the supported browser -> Odoo -> durable turn -> Codex path, the Assistant executed only
`assistant.conversation.set_response_language` and changed one conversation to `fixed/en`. The
next turn captured `fixed/en` and produced the requested English marker. A new conversation
captured `inherit` with no fixed language and produced the requested isolation marker.

The sanitized persistence review confirmed:

- the preference turn retained its creation-time `inherit` snapshot;
- its durable working items contained the governed capability and a verified effect receipt;
- the following turn in the same conversation captured `fixed/en` in both the turn fields and
  bounded session settings;
- the new conversation remained `inherit`, proving conversation isolation.

## Corrections and acceptance boundary

The focused lifecycle-test cursor correction is already present and published at the tested
checkpoint (`faf21f4`). This rerun found no additional defect in the requested scope, so no product
code was changed.

The disposable database, filestore, temporary database role/configuration and browser session were
removed after evidence collection. This PASS does not mark P5.8 `COMPLETE`; the remaining HARD
gates continue to be governed by `P5.8_VALIDATION_RUNBOOK.md`.
