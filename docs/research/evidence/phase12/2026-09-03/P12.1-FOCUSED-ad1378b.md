# P12.1 focused acceptance — ad1378b

Date: 2026-09-03
Status: **PASS / P12.1 ACCEPTED / P12.2 ELIGIBLE**

## Tested lineage

```text
ad1378be0836fa3d49e4f24019288aa3a6e71b46
```

The final tested checkout was clean `main`, synchronized with `origin/main`. The P12.1
runtime, ADR and prepared tests arrived through `f8c60bb`; `cf747f8` contains the
three mechanical lint repairs found by the initial formal gate and `ad1378b` strengthens
the declared authority coverage without changing runtime behavior.

## Environment

- Odoo 18 Community on disposable database
  `odoo_ai_p12_1_focus_20260903_cf747f8`.
- Python 3.12.3 and PostgreSQL 16.15 on Ubuntu 24.04/WSL2.
- Installed addon version `18.0.13.33.0`.
- Exact addon paths:
  `/odoo/odoo-server/addons,/odoo/custom/addons/odoo-ai-assistant/addons`.
- No provider login, production database, GitHub Actions or privileged broker was
  required for this focused source-workspace authority gate.

## Initial failure and repair

The first formal static run stopped before tests because Ruff found three mechanical
issues:

```text
UP037  unnecessary quoted future annotation
RUF022 unsorted __all__
I001   unformatted long Odoo test import
```

Ruff applied only those fixes. No workspace behavior, authority, ceiling or public
contract changed. The tests were then strengthened to assert private modes, every
configured size class, source-root symlinks, path/mtime-independent fingerprints, all
binding components and absence of P12 edit capabilities. The complete gate was
restarted on `ad1378b`.

## Static and dependency-light gate

Executed surfaces:

```text
addons/odoo_ai_assistant/runtime/source_workspace.py
tests/unit/test_phase12_source_workspace.py
addons/odoo_ai_assistant/tests/test_phase12_source_workspace.py
addons/odoo_ai_assistant/tests/__init__.py
addons/odoo_ai_assistant/__manifest__.py
```

Results:

```text
Python compileall                                      PASS
Ruff                                                   PASS — All checks passed
SourceWorkspaceTests                                   PASS — 10/10
git diff --check                                       PASS
```

The 10 tests cover host-generated ids, deterministic path/mtime-independent
fingerprints, independent workspace/source changes, source immutability across
prepare/inspect/delete, private `0700`/`0600` modes, traversal and malformed-id
rejection, root overlap, root/entry symlinks, file-count/single-file/total-byte bounds,
secret handling, every binding component and bounded deletion.

## Focused Odoo authority gate

Command:

```bash
/odoo/venv/bin/python3 /odoo/odoo-server/odoo-bin \
  --config=/etc/odoo-server.conf \
  --database=odoo_ai_p12_1_focus_20260903_cf747f8 \
  --addons-path=/odoo/odoo-server/addons,/odoo/custom/addons/odoo-ai-assistant/addons \
  --update=odoo_ai_assistant \
  --test-enable \
  --test-tags=/odoo_ai_assistant:TestPhase12SourceWorkspace \
  --without-demo=all --stop-after-init
```

Result: **PASS — 3 selected methods, 0 failures, 0 errors**. The addon was first
installed cleanly during the initial committed lint-repair run; the final `ad1378b`
update loaded the strengthened tests and executed 354 queries in 1.88 seconds.

The Odoo gate proves:

- a Technical user can prepare and inspect a path-free snapshot of the installed
  `odoo_ai_assistant` addon;
- public metadata exposes only logical identities, counts and fingerprints;
- a normal internal user cannot prepare a workspace;
- another Technical user or another turn cannot reuse the workspace id;
- the owner can delete the workspace through the bound adapter; and
- P12.1 registers no source-edit, patch, test-execution or deployment capability.

## Scope honesty

This is focused **P12.1 slice acceptance**, not full Phase-12 acceptance and not a
production-deployment authorization. The five Phase-12 real gates remain unexecuted
until their P12.2-P12.4 contracts exist:

```text
P12-REAL-PATH-BOUNDARY
P12-REAL-DIFF-APPROVAL
P12-REAL-TEST-BEFORE-DEPLOY
P12-REAL-DEPLOY-VERIFY
P12-REAL-FAILED-DEPLOY-RECOVERY
```

The full repository, full addon, HOOT/browser and Product Behavior suites were not
executed; they remain periodic validation debt because no focused failure established
a wider blast radius.

## Decision

The committed-SHA dependency-light/static gate and focused Odoo authority gate are
green. P12.1 is accepted and P12.2 may begin defining a typed proposed patch/diff
contract restricted to the bound private workspace. Installed source remains outside
the ordinary Odoo mutation boundary.
