# P0.2 — READ acceptance evidence

Date: 2026-08-27  
Inspected HEAD: `43468fa7e511d4a6a8b2d448a1cf06a2a812a089`  
Implementation commit: `fee7c4ee8b532e4529ad5dfd6249caab2c877a88`  
Status: `COMPLETE`

## Objective

Prevent a Phase 0 READ capture from being treated as functional success merely because the Odoo turn reached `completed`.

The first real `read_partner` capture completed without any capability/tool checkpoints and the browser answer reported that the Odoo query failed. That observation remains a failed `P0-REAL-READ`.

## Change

Added `tests/e2e/phase0_read_acceptance.py` plus `tests/unit/test_phase0_read_acceptance.py`.

For `read_partner`, `query_sales`, and `aggregate_sales`, the acceptance evaluator now requires all of:

- `capture_kind=live_http`;
- no request-side or capture-side error;
- final state `completed`;
- observed `tool.started`;
- observed `tool.completed`.

The evaluator consumes only already-sanitized Phase 0 traces. It does not inspect prompt text, answer text, raw tool arguments/results, provider output, credentials, or private reasoning. It does not modify product runtime behavior.

This is intentionally a minimum machine gate. The real validation still requires a human/product check that the final answer reflects the known Odoo data, per `P0-REAL-READ`.

## Deterministic validation

Executed against the exact published implementation contents in an isolated executable tree:

```text
python -m pytest -q tests/unit/test_phase0_read_acceptance.py
...                                                                      [100%]
3 passed in 0.05s

python -m py_compile tests/e2e/phase0_read_acceptance.py
PASS (exit 0, no output)
```

Covered regressions:

1. a `completed` apology with no tool evidence is rejected;
2. a completed READ with `tool.started` + `tool.completed` is accepted by the machine evidence gate;
3. an interrupted capture is rejected even if tool events were already observed.

## Validation debt

### VD-P0.2-REAL-READ — CLOSED

```text
validation_id: P0-REAL-READ
gate_type: HARD
origin_slice: P0.2-read-failure-diagnosis
commit_materially_tested: a05e75006f53b056f31ab96c3864092d89199480
result: PASS
evidence:
  - read-partner-a05e750.json
  - read-partner-a05e750-acceptance.json
remaining_guard: P0.3 must bound the provider/Odoo crash path before ACTION is retried
```

## Validation attempt at current main

Checkpoint inspected: `ffa8dfd04449bcd04e95f27519e455f5d314b9ea`.

The deterministic gate was rerun successfully:

```text
.venv/bin/python -m pytest -q tests/unit/test_phase0_read_acceptance.py
3 passed in 0.02s

.venv/bin/python -m py_compile \
  tests/e2e/phase0_read_acceptance.py tests/e2e/phase0_live_capture.py
PASS
```

The evaluator was also run against the existing failed real READ capture. It returned the expected
exit status `2`, `accepted=false`, final state `completed`, and both required tool events missing.
This proves the false-positive regression is rejected but is not a new real READ PASS.

A fresh `P0-REAL-READ` could not be executed from this session:

- `http://127.0.0.1:8069` was reachable but exposed `odoo_ai_m1_08_9473`, not the recorded Phase 0
  evidence database `codex_m7_odoo_test`;
- the active Odoo process started before the current checkout and could not be verified as running
  the exact commit;
- the required Phase 0 login, password, message and screen inputs were not present;
- the service configuration was not readable by the current user and the standard `admin`/`admin`
  probe did not authenticate.

No live capture was created and `VD-P0.2-REAL-READ` remains open. The next run needs the matching
disposable environment, capture credentials and permission to update/restart Odoo at current main.

That blocker was subsequently resolved in the same local execution environment by using root WSL
service authority, updating the addon, restarting Odoo and creating isolated disposable fixtures.

## Successful real validation

```text
validation_id: P0-REAL-READ
commit_tested: a05e75006f53b056f31ab96c3864092d89199480
date: 2026-08-27
odoo_version: 18.0
codex_version: 0.144.2
database: odoo_ai_m1_08_9473 (local disposable/adapted environment)
user_profile: isolated internal admin-equivalent validation user
result: PASS
```

The addon was updated from the exact commit and `odoo-server.service` was restarted before the
capture. A dedicated internal user and partner were created for this validation; the generated
login password existed only in process memory and is not present in the evidence. The temporary
user was deactivated after the browser-history check; the non-sensitive partner fixture remains
available for bounded follow-up validation.

Observed:

- final state `completed` with no request, capture or terminal product error;
- two `tool.started`/`tool.completed` pairs were observed;
- machine evaluator returned exit `0`, `accepted=true`, `missing_tool_events=[]`;
- the authenticated `/odoo_ai/v1/chat-history` browser payload contained the exact fixture name and
  email stored in Odoo;
- browser submit-to-persist was 383.163 ms and browser final was 16,120.006 ms;
- provider process start was 2.805 ms, provider initialization 192.119 ms, first provider event
  1,125.771 ms and first answer delta 14,568.026 ms after runtime start;
- `odoo-server.service` remained active with `NRestarts=0` during the READ validation.

Sanitized artifacts:

- `read-partner-a05e750.json` — SHA-256
  `01cdbd2cba0d6c7d11bec60d5d2f4c19283170619861839b36a55e2afaa7a396`;
- `read-partner-a05e750-acceptance.json` — SHA-256
  `7515157508945bdf4bb1d85caa3e9c42b2b9c38fcab56869f5cbf38cb1427862`.

The artifacts contain no password, prompt, answer body, raw tool/provider payload, credential or
private reasoning. P0.2 is complete. ACTION remains frozen pending P0.3.

## Exact real validation

On an environment running the exact current `main`:

1. update/restart the Odoo 18 addon from that commit and verify Codex is connected;
2. use the known disposable/demo partner from the Phase 0 environment;
3. capture `read_partner` with `tests/e2e/phase0_live_capture.py` while asking for a small set of non-sensitive fields;
4. run `tests/e2e/phase0_read_acceptance.py <capture.json> --out <acceptance.json>`;
5. require exit status `0`, `accepted=true`, and no missing tool events;
6. inspect the browser answer and verify that it reflects the actual known partner data;
7. confirm the capture contains queue/provider/tool/finalization timing needed by `P0-REAL-READ`.

If the evaluator fails, use the sanitized trace to distinguish provider/account/runtime failure from missing capability selection or capability execution failure. Do not retry ACTION while this hard gate is failing.
