# P0.4 — bounded provider fault-injection fixtures

Date: 2026-08-27  
Inspected HEAD: `ce794792be40b6d5420752c2f2c7530e35135eca`  
Status: `COMPLETE`

## Objective

Provide bounded, deterministic provider fault fixtures for the three Phase 0 failure paths that were previously named but not safely injectable: provider disconnect/EOF, provider timeout, and invalid final provider output.

This slice is measurement/test infrastructure only. It does not change the embedded product runtime, provider lifecycle, capability framework, Odoo authority, ACLs, approvals, writes or recovery.

## Current-code basis

The fixtures target the current embedded contracts rather than the retired sidecar:

- `runtime/account.py` authenticates the executable through App Server `initialize` + `account/read`;
- `runtime/agent/codex.py` starts a fresh App Server for reasoning and fails closed on EOF, read timeout and malformed final JSON;
- `models/turn_queue.py` keeps those provider errors retryable only while no write barrier exists;
- ADR-016 keeps Codex ephemeral and host-authorized; the model/provider never gains direct ORM, shell, SQL or write authority.

## Implemented fixtures

Added:

```text
tests/fixtures/phase0_codex_fault_app_server.py
tests/fixtures/codex_phase0_eof
tests/fixtures/codex_phase0_timeout
tests/fixtures/codex_phase0_invalid_output
tests/fixtures/phase0_provider_fault_manifest.json
tests/unit/test_phase0_codex_fault_fixture.py
```

The common fake App Server implements only the minimum fixed protocol needed by the current account gate and reasoning client. It never echoes request content and never emits a dynamic tool request.

### EOF

`codex_phase0_eof` passes the account gate with a fixed non-secret fixture account, passes initialize/thread/start/turn start, then exits immediately after acknowledging `turn/start`.

Expected originating code: `codex_process_eof`.

### Timeout

`codex_phase0_timeout` distinguishes the account client from the reasoning client using the existing `clientInfo.name` values. Account initialization remains healthy. Reasoning initialization remains silent, so the current host-owned 5-second startup timeout bounds each failed attempt.

Expected originating code: `codex_read_timeout`.

This deliberately avoids a 120-second silent turn fixture and therefore keeps the Phase 0 trial bounded even with current retry semantics.

### Invalid final output

`codex_phase0_invalid_output` completes a provider turn with a fixed `agentMessage` whose text is `not-json`. It performs no tool call and supplies no business data.

Expected originating code: `codex_answer_invalid`.

## Manifest

`phase0_provider_fault_manifest.json` binds each scenario to its executable, expected terminal `failed` state and expected original error code. It is fixture metadata only; it does not alter the product failure taxonomy.

## Deterministic validation

Actually executed against the exact fixture/test contents in an isolated executable tree:

```text
python -m py_compile \
  tests/fixtures/phase0_codex_fault_app_server.py \
  tests/unit/test_phase0_codex_fault_fixture.py
PASS

python -m pytest -q tests/unit/test_phase0_codex_fault_fixture.py
6 passed in 12.49s
```

The tests cover fixed authenticated account response without secret/request echo, EOF sequence, bounded reasoning-initialize timeout while the account gate remains healthy, malformed final-output sequence, executable wrapper/version/stdio behavior, and manifest completeness/error-code bindings.

The execution host also emitted unrelated spreadsheet-runtime warmup diagnostics on stderr; the validation commands themselves exited 0.

Revalidation after pulling `9008821`:

```text
python -m pytest -q tests/unit/test_phase0_codex_fault_fixture.py
6 passed in 0.06s

python -m pytest -q tests/unit/test_phase0_*.py tests/unit/test_codex_provider_conformance.py
33 passed in 0.12s
```

## Real validation result

Environment:

```text
commit_tested: 90088215f247716b57e5c19c2502cc2d33a78e51
odoo_version: 18.0 Community
addon_version: 18.0.10.4.6
browser: Google Chrome 151.0.7922.174, headless
user_profile: disposable internal user
```

All three named checks passed:

| Validation | Original code | Final state | Observed UI category | Result |
| --- | --- | --- | --- | --- |
| `P0.4-REAL-PROVIDER-EOF-PAIR` | `codex_process_eof` | `failed` | `service_unavailable` | PASS |
| `P0.4-REAL-PROVIDER-TIMEOUT-PAIR` | `codex_read_timeout` | `failed` | `service_unavailable` | PASS |
| `P0.4-REAL-INVALID-OUTPUT-PAIR` | `codex_answer_invalid` | `failed` | `service_unavailable` | PASS |

For each case Chrome submitted a separate real panel turn, observed
`.o_ai_assistant_failure_message`, and intercepted the terminal status only to confirm the exact
backend code/state. A second bounded turn produced the sanitized `phase0_live_capture.py` artifact
with the previously observed UI category. The visible message key and SHA-256 were identical for
all three cases:

```text
message_key: assistant_failure_service_unavailable
message_sha256: 27ec69cc9a45110e7b39284a7e98ae4c1448669fc3efcaaf2dbfb98bb449e4be
```

This confirms the measurement described by the playbook: three distinct provider failures are
flattened to one browser category. P0.4 records that behavior; changing the failure contract belongs
to the later failure-contract phase.

The executable override was restored after every case and the real Codex account returned to
`authenticated`. Odoo stayed `active/running` with stable PID/start identity during every measured
trial. Administrative restarts were performed before each measurement because the long-running
HTTP process cached the preceding `ir.config_parameter` value; those planned setup restarts are not
fixture-induced failures. No dynamic tool, business write, approval or recovery path occurred.

Sanitized artifacts:

- `p0.4-provider-disconnect-9008821.json`;
- `p0.4-provider-timeout-9008821.json`;
- `p0.4-invalid-final-output-9008821.json`;
- `p0-browser-ui-observations-9008821.json`.

The remaining fifth Phase 0 pair was also completed safely by disabling only the database-scoped
connection gate, without logging out or modifying provider credentials:

```text
provider_auth_missing: codex_not_connected -> codex_not_connected
```

Together with the existing `provider_process_missing` pair, the aggregate report now counts five
distinct failure paths. It still exits `2` because the ACTION baseline is missing.

## Real procedure

Use only the disposable Odoo environment. Record the current value of `odoo_ai_assistant.codex_executable` first and restore it after every fixture.

For each fixture, set the administrative executable override to the absolute executable path in the checked-out repository. No provider credentials are required by the fake fixture and no real account material is copied.

```text
provider_disconnect   -> tests/fixtures/codex_phase0_eof
provider_timeout      -> tests/fixtures/codex_phase0_timeout
invalid_final_output  -> tests/fixtures/codex_phase0_invalid_output
```

Then run the existing normal-user HTTP capture:

```bash
python tests/e2e/phase0_live_capture.py \
  --scenario <scenario> \
  --out /tmp/phase0/<scenario>.json
```

Observe the final browser error/category and rerun/save the capture with `--ui-error-code` only after that UI value was actually observed.

Stop immediately if a business write/approval path appears, Odoo becomes unhealthy or restarts, the fixture executable is not the exact intended absolute path, or the observed original error differs from the manifest.

After each trial restore the previous Codex executable override and confirm normal provider status.

## Exit rule

P0.4 is complete. ACTION remains separate; no write PASS is inferred from these fault-fixture
results. The next hard gate is the safe disposable `P0-REAL-ACTION` rerun.
