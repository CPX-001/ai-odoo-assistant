# Historical M5 task-packet index

> Archive notice: this directory records the former M5 QUERY/HOW_TO/RAG milestone. It is not the current routing or retrieval specification.

Some concepts survived in redesigned form—schema-first bounded Odoo queries and installation-aware guidance—but the rigid workflow/router and the sidecar PostgreSQL FTS knowledge stack are retired from the active product path.

The packets remain useful as design/test evidence. Do not treat completed M5 RAG, `QUERY`/`HOW_TO` workflow routing, sidecar stores or service APIs as features currently exposed by the embedded runtime.

Current replacements:

- query contract: `../../../QUERY_CONTRACT.md`;
- HOW_TO behavior: `../../../HOW_TO_WORKFLOW.md`;
- retrieval status/gaps: `../../../KNOWLEDGE_INDEX.md`;
- unified runtime: `../../../UNIFIED_AGENT_RUNTIME.md` and ADR-014/ADR-016.

The parent archive policy is `../README.md`.
