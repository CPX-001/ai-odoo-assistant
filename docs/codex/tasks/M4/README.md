# Historical M4 task-packet index

> Archive notice: this directory records the former M4 Codex vertical-slice milestone. It is protocol and implementation history, not the current agent-runtime specification.

M4 introduced the first Codex App Server integration and read-only agentic flow. Codex App Server remains the current reasoning-provider adapter, but its surrounding host, persistence, tool exposure and execution model have changed materially since this milestone.

Use the packets to understand earlier protocol decisions and tests, then re-check current adapter code and upstream Codex behavior before making changes.

Current replacements:

- Codex integration: `../../README.md` and `../../CODEX_AUTH.md`;
- agent lifecycle: `../../../UNIFIED_AGENT_RUNTIME.md`;
- capability contract: `../../../CAPABILITY_FRAMEWORK.md`;
- architecture/current state: `../../../ARCHITECTURE.md`, `../../../CURRENT_STATE.md`.

The parent archive policy is `../README.md`.
