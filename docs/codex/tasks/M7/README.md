# Historical M7 task-packet index

> Archive notice: this directory records the former M7 product-hardening sequence. Its incomplete milestone state is historical; unstarted packets are **not** the current backlog.

M7 work contributed settings, diagnostics and operational-hardening ideas, but the old milestone was superseded by the later embedded-runtime refactor and subsequent ADRs. Current code must be inspected feature by feature rather than inferring implementation from the old M7 completion/gate state.

Do not resume `M7-06+`, restore sidecar maintenance flows or treat `docs/OPERATIONS_M1.md`/the old Source-of-Truth PDF as current authority.

Current replacements:

- implementation snapshot: `../../../CURRENT_STATE.md`;
- deployment/configuration: `../../../DEPLOYMENT_CONFIG.md`;
- chat/product flow: `../../../CHAT_PRODUCT_FLOW.md`;
- Codex account lifecycle: `../../CODEX_AUTH.md`;
- current architecture/ADRs: `../../../ARCHITECTURE.md`, `../../../adr/README.md`.

The packets remain unchanged as historical evidence. The parent archive policy is `../README.md`.
