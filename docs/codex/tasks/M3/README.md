# Historical M3 task-packet index

> Archive notice: this directory records the former M3 source/log evidence milestone. It is not evidence that the same retrieval stack exists in the current embedded runtime.

M3 implemented sidecar-era source indexing and log retrieval/diagnostics. Those artifacts are valuable when redesigning runtime/source/log evidence, but current `main` must be inspected before assuming a provider, scanner, store or API still exists.

The packets remain unchanged as historical evidence. Do not reconnect the retired service implementation merely because a feature was previously completed there.

Current replacements and status:

- implementation snapshot: `../../../CURRENT_STATE.md`;
- retrieval status: `../../../KNOWLEDGE_INDEX.md`;
- architecture: `../../../ARCHITECTURE.md`;
- diagnostics/runtime code: inspect current addon models/runtime first.

The parent archive policy is `../README.md`.
